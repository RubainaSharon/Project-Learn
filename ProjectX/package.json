{
  "name": "quiz-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.7.7",              // Latest version compatible with Node.js 18.x
    "canvas-confetti": "^1.6.0",     // Already compatible
    "react": "^18.3.1",              // Latest stable version
    "react-quill": "^2.0.0",         // Already compatible
    "react-icons": "^5.3.0",         // Latest version
    "react-dom": "^18.3.1",          // Latest stable version
    "react-router-dom": "^6.26.2",   // Latest version
    "react-scripts": "^5.0.1",       // Keep as-is for now; may need to migrate if issues persist
    "react-syntax-highlighter": "^15.6.1", // Already compatible
    "web-vitals": "^4.2.3"           // Latest version
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "engines": {
    "node": "18.x"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",      // Latest compatible version
    "postcss": "^8.4.47",            // Latest version
    "tailwindcss": "^3.4.14"         // Latest version
  }
}
